*.pyc

# pyinstaller
dist
build

.idea
.vscode

.coverage
test-reports

jabs.venv
venv
.venv

.DS_Store
*.sif

temp
data

tests/testing_notebook.ipynb
docs/notes.txt
docs/work_history.md
docs/drafts/
site/
scratch/

# Agent worktrees. Each is its own git repository, so `git add -A` otherwise
# commits one as a gitlink. `.claude/settings.json` stays tracked.
.claude/worktrees/
